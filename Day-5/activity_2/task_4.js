// Write a function expression to concatenate two strings.

function concatenateString(str1, str2) {
  const mergeString = str1 + str2;
  return mergeString;
}

const result = concatenateString("chai", "code");

console.log({ result });
