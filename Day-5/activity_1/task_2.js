// Write a function to calculate square of a number.

function findSquare(input) {
  const inputRes = input * input;

  return "Square of " + input + " is " + inputRes;
}

const result = findSquare(55);

console.log({ result });
