//Write a program that uses the ternary operator to check if a number is positive or negative and log the result to the console.

const operand1 = 20;

const operand2 = -20;

const result1 = operand1 > 0 ? "Positive" : "Negative";
const result2 = operand2 > 0 ? "Positive" : "Negative";

console.log("Result after using ternary operator for 20 is: " + result1);
console.log("Result after using ternary operator for -20 is: " + result2);
